/**
 * Técnico home placeholder. The `borrador` form, autosave, review gate and
 * signing flow (D3, D5, D6, D9–D14) land in later slices; this only proves
 * a real `tecnico` session reaches the guarded route tree.
 */
export function InicioTecnico() {
  return <p>Visita en curso</p>;
}
